import "package:flutter/material.dart";

class Second extends StatelessWidget{
  @override
  Widget build(BuildContext build){
    return new Container(
      child: new Center(
        child: new Icon(Icons.message,size:40.0,color: Colors.blueAccent),
      ),
    );
  }
}
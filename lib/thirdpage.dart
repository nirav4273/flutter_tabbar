import "package:flutter/material.dart";

class Third extends StatelessWidget{
  @override
  Widget build(BuildContext build){
    return new Container(
      child: new Center(
        child: new Icon(Icons.games,size:40.0,color: Colors.greenAccent),
      ),
    );
  }
}